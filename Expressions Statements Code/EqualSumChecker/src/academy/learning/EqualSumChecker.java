package academy.learning;

public class EqualSumChecker {
    public static boolean hasEqualSum(int sz1, int sz2, int sz3) {
        if (sz1 + sz2 == sz3) {
            return true;
        } else {
            return false;
        }
    }
}
